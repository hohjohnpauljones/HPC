#include "hw5.hpp"

void row_producer(string fname, std::vector<Row> * rows)
{
	/*
	while(!eof)
	v = readline
	row temp;
	temp.x = v[1];
	temp.y = v[2];
	for (i = 0; i < 360;)
		temp.data[i] = v[i+2];
	init temp.result
	rows.push(temp);
	signal sem_avail;
	*/
}